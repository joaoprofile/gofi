# Índice de PRDs — manifesto de retrieval (RAG)

> **Derivado do frontmatter.** Carregue este arquivo para **descobrir** qual doc cobre um
> tema (case por `keywords`), depois leia só o frontmatter do doc-alvo e pule para a
> §seção certa. Protocolo completo em `.claude/knowledge/shared/rag-retrieval-protocol.md`.
>
> **Não edite a tabela à mão.** Regenere: `bash .claude/scripts/gen-index.sh prd`

| Contexto | Submódulo | Versão | Status | Keywords | Arquivo |
|----------|-----------|--------|--------|----------|---------|
<!-- vazio — o primeiro PRD criado por /gofi-pd popula esta tabela: bash .claude/scripts/gen-index.sh prd -->
