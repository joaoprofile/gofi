# {{.ProjectName}}

Rust service bootstrapped by `gofi init`.

## Layout

```
{{.SourceRoot}}/
├── Cargo.toml                # workspace
├── .migrations/
└── {{.ProjectName}}/
    ├── Cargo.toml
    └── src/
        ├── main.rs
        └── domain/           # one module per bounded context
```

## Build & run

```sh
cargo run --manifest-path {{.SourceRoot}}/Cargo.toml -p {{.ProjectName}}
```

## Tests

```sh
gofi test
```
