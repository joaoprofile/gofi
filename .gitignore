/bin/
/dist/
*.test
*.out
coverage.html
.env
.env.local

# Copiado pelo goreleaser before-hook a partir da raiz
/cli/README.md

# contexto da CLI, gerado ao lado do executável
gofi.json

# Resumo de apresentação — material local, não versionado
/VISAO-GERAL.md

# Fixture de teste do chat: um .gofi.yaml na raiz faz este repo se identificar
# como um projeto gofi, o que ele não é
/.gofi.yaml
/_new_.gofi.yaml
